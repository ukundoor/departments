<h1>Listing employees</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Age</th>
      <th>Salary usd</th>
      <th>Department</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @employees.each do |employee| %>
      <tr>
        <td><%= employee.name %></td>
        <td><%= employee.email %></td>
        <td><%= employee.age %></td>
        <td><%= employee.salary_usd %></td>
        <td><%= employee.department.name %></td>
        <td><%= link_to 'Show', employee %></td>
        <td><%= link_to 'Edit', edit_employee_path(employee) %></td>
        <td><%= link_to 'Destroy', employee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'orderbyage', orderbyage_path %>
<%= link_to 'orderbydep', orderbydep_path %>
<%= link_to 'orderbysalary', orderbysalary_path %>
<%= link_to 'orderbyretire', orderbyretire_path %>
<%= link_to 'highpay', highpay_path %>
<%= link_to 'top5salary', top5salary_path %>
<%= link_to 'top5seniors', top5seniors_path %>
<%= link_to 'highandlowsalary', highandlowsalary_path %>



<%= link_to 'New Employee', new_employee_path %>
